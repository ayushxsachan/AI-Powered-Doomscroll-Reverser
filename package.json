{
  "name": "doomscroll-reverser",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack AI-powered productivity horror app that reverses doomscrolling into opportunity cost.",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "lint:frontend": "npm --prefix frontend run lint",
    "build:frontend": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0",
    "python": ">=3.11"
  }
}

